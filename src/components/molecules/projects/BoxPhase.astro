---
const { 
  text, 
  icon: Icon, 
  color
  } = Astro.props;


---

<div class="box"  style={`--bg-color: ${color}`}
>
  <Icon class="box__icon" alt="icono" />
  <p class="box__text">{text}</p>
</div>

<style lang="scss" scoped>

.box {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: end;
  width: 9.25rem;
  height: 9.25rem;
  padding: 1rem;
  background-color: var(--bg-color);
  border-radius: 1rem;
  color: $color-white;

  &__icon {
    max-width: 4rem;
    height: auto;
  }

  &__text {
    font-family: $font-poppins;
    font-size: $font-size-md;
    font-weight: 500;
    text-align: center;
    margin-top: 1rem;
  }

  @media (max-width: 768px) {
    width: 7.25rem;
    height: 7.25rem;

    &__icon {
      max-width: 2rem;
    }

    &__text{
      font-size: $font-size-sm;
    }
  }


}
</style>
